[pytest]
markers =
    llm: tests that require LLM integration
    unit: tests that don't require external services
testpaths = 
    tests/unit 
    tests/llm
python_files = test_*.py
python_classes = Test*
python_functions = test_* 